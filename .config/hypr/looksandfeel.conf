source = ~/.cache/hellwal/colors-hyprland.conf
# Refer to https://wiki.hyprland.org/Configuring/Variables/

# https://wiki.hyprland.org/Configuring/Variables/#general
general { 
    gaps_in = 5
    gaps_out =2
    col.inactive_border = $colorborder
    col.active_border = $coloractiveborder
    border_size =2
    # https://wiki.hyprland.org/Configuring/Variables/#variable-types for info about colors
    # Set to true enable resizing windows by clicking and dragging on borders and gaps
    resize_on_border = true    # Please see https://wiki.hyprland.org/Configuring/Tearing/ before you turn this on
    allow_tearing = false
    layout = dwindle
}

# https://wiki.hyprland.org/Configuring/Variables/#decoration
decoration {

  dim_special = 0
    inactive_opacity =0.8
    active_opacity = 1
    rounding = 15
  shadow {
  color = $colorshadow
  color_inactive = $colorshadowinactive
    # Change transparency of focused and unfocused windows
    range = 30
    render_power = 4
  }
   blur {
        enabled = true
        xray = false
        size = 3
        contrast = 3.2
        passes = 3
        new_optimizations = true
special = false
    }
}

# https://wiki.hyprland.org/Configuring/Variables/#animations
animations {
    enabled = true
    
    # === CURVAS BÉZIER SUAVES E FLUIDAS ===
    bezier = myBezier, 0.05, 0.9, 0.1, 1.05
    bezier = smoothOut, 0.25, 0.46, 0.45, 0.94
    bezier = smoothIn, 0.55, 0.06, 0.68, 0.19
    bezier = overshoot, 0.05, 0.9, 0.1, 1.1
    bezier = energetic, 0.25, 0.46, 0.45, 0.94
    bezier = snappy, 0.4, 0.0, 0.2, 1.0
    bezier = bouncy, 0.68, -0.55, 0.265, 1.55
    bezier = quick, 0.25, 0.46, 0.45, 0.94
    bezier = fluid, 0.23, 1, 0.32, 1
    bezier = smooth, 0.25, 0.1, 0.25, 1

    # === ANIMAÇÕES DE JANELAS (FLUIDAS) ===
    animation = windows, 1, 4, fluid
    animation = windowsOut, 1, 3, snappy, popin 70%
    animation = windowsIn, 1, 4, bouncy, popin 70%
    animation = windowsMove, 1, 3, smooth
    
    # === ANIMAÇÕES DE BORDAS (SUAVES) ===
    animation = border, 1, 4, smooth
    animation = borderangle, 1, 4, fluid
    
    # === ANIMAÇÕES DE FADE (FLUIDAS) ===
    animation = fade, 1, 3, smooth
    animation = fadeIn, 1, 3, fluid
    animation = fadeOut, 1, 3, snappy
    animation = fadeSwitch, 1, 3, smooth
    animation = fadeShadow, 1, 3, fluid
    animation = fadeDim, 1, 3, smooth
    
    # === ANIMAÇÕES DE WORKSPACES (SUBTIS) ===
    animation = workspaces, 1, 3, smoothOut, slidefade 15%
    animation = workspacesIn, 1, 3, smoothIn, slidefade
    animation = workspacesOut, 1, 3, smoothOut, slidefade
    animation = specialWorkspace, 1, 3, smoothOut, slidefade
    animation = specialWorkspaceIn, 1, 3, smoothIn, slidefade
    animation = specialWorkspaceOut, 1, 3, smoothOut, slidefade
    
    # === ANIMAÇÕES DE LAYERS (FLUIDAS) ===
    animation = layers, 1, 3, smooth, slide
    animation = layersIn, 1, 3, fluid, slide
    animation = layersOut, 1, 3, snappy, slide
    
    # === ANIMAÇÕES DE POPUPS WAYLAND (SUAVES) ===
    animation = fadeLayers, 1, 3, smooth
    animation = fadeLayersIn, 1, 3, fluid
    animation = fadeLayersOut, 1, 3, snappy
    animation = fadePopups, 1, 3, smooth
    animation = fadePopupsIn, 1, 3, fluid
    animation = fadePopupsOut, 1, 3, snappy
    
    # === ANIMAÇÕES ESPECIAIS (FLUIDAS) ===
    animation = zoomFactor, 1, 4, smooth
    animation = monitorAdded, 1, 4, fluid
    animation = fadeDpms, 1, 3, smooth
}
# See https://wiki.hyprland.org/Configuring/Dwindle-Layout/ for more
dwindle {
    pseudotile = true # Master switch for pseudotiling. Enabling is bound to mainMod + P in the keybinds section below
    preserve_split = true # You probably want this
}


# https://wiki.hyprland.org/Configuring/Variables/#misc
misc { 
    force_default_wallpaper = 1 # Set to 0 or 1 to disable the anime mascot wallpapers
    disable_hyprland_logo = true
    mouse_move_focuses_monitor = true
    disable_splash_rendering = false
    focus_on_activate = true
    vfr = true
}


#############
### INPUT ###
#############

# https://wiki.hyprland.org/Configuring/Variables/#input
input {
    kb_layout = br, us
    kb_options=grp:alt_space_toggle
    kb_variant =
    kb_model =
    kb_rules =
    follow_mouse = 1

    sensitivity = 0 # -1.0 - 1.0, 0 means no modification.

    touchpad {
        natural_scroll = false
    }
    force_no_accel = true
    numlock_by_default = true
}

# https://wiki.hyprland.org/Configuring/Variables/#gestures
gestures {
    # === GESTURES BÁSICOS ===
    
    # Swipe para trocar workspaces (direções específicas)
    gesture = 3, left, workspace
    gesture = 3, right, workspace
    
    # Swipe para resize de janelas (horizontal)
    gesture = 3, horizontal, resize
    
    # Swipe para mover janelas (vertical) 
    gesture = 3, vertical, move
    
    # === GESTURES COM MODIFICADORES ===
    
    # Swipe para fechar janela (3 dedos para baixo + ALT)
    gesture = 3, down, mod:ALT, close
    
    # Swipe para fullscreen (3 dedos para cima + SUPER)
    gesture = 3, up, mod:SUPER, scale:1.5, fullscreen
    
    # Swipe para float/tile (3 dedos esquerda/direita + SUPER)
    gesture = 3, left, mod:SUPER, scale:1.2, float
    gesture = 3, right, mod:SUPER, scale:1.2, float
    
    # === GESTURES DE PINCH ===
    
    # Pinch para fullscreen (2 dedos)
    gesture = 2, pinchout, fullscreen
    gesture = 2, pinchin, fullscreen
    
    # === GESTURES ESPECIAIS ===
    
    # Swipe para special workspace (4 dedos)
    gesture = 4, left, special, scratchpad
    gesture = 4, right, special, scratchpad
}

# Example per-device config
# See https://wiki.hyprland.org/Configuring/Keywords/#per-device-input-configs for more
device {
    name = epic-mouse-v1
    sensitivity = -0.5
}

ecosystem {
}


permission = .*wl-kbptr.*, screencopy, allow

render{
direct_scanout	= 2
}
