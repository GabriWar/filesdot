env = LIBVA_DRIVER_NAME,nvidia
env = __GLX_VENDOR_LIBRARY_NAME,nvidia
env = __GL_VRR_ALLOWED,1
env = WLR_NO_HARDWARE_CURSORS,1
env = WLR_DRM_NO_ATOMIC,1
env = XDG_CURRENT_DESKTOP,sway
env = GBM_BACKEND,nvidia-drm
env = XDG_SESSION_TYPE,wayland
env = TERMINAL,kitty
env = terminal,kitty
env = BROWSER,thorium-browser
env = browser,thorium-browser
env = HYPRCURSOR_THEME,Bibata-Modern-Ice
env = HYPRCURSOR_SIZE,10
env = QT_QPA_PLATFORMTHEME,qt5ct
env = QT_STYLE_OVERIDE,GTK+
env = EDITOR,nvim
env = GDK_SCALE,1
env = QT_AUTO_SCREEN_SCALE_FACTOR,1
source = ~/.cache/wal/colors-hyprland.conf
# #######################################################################################
# AUTOGENERATED HYPR CONFIG.
# PLEASE USE THE CONFIG PROVIDED IN THE GIT REPO /examples/hypr.conf AND EDIT IT,
# OR EDIT THIS ONE ACCORDING TO THE WIKI INSTRUCTIONS.
# #######################################################################################
debug{
disable_logs = false
enable_stdout_logs = true
}

# This is an example Hyprland config file.
# Refer to the wiki for more information.
# https://wiki.hyprland.org/Configuring/Configuring-Hyprland/

# Please note not all available settings / options are set here.
# For a full list, see the wiki

# You can split this configuration into multiple files
# Create your files separately and then link them to this file like this:
# source = ~/.config/hypr/myColors.conf


################
### MONITORS ###
################

# See https://wiki.hyprland.org/Configuring/Monitors/
monitor=,preferred,auto,auto
monitor= HDMI-A-1,1920x1080@60,auto,1,mirror,eDP-1
###################
### MY PROGRAMS ###
###################

# See https://wiki.hyprland.org/Configuring/Keywords/

# Set programs that you use
$terminal = kitty
$fileManager = krusader 
$menu = rofi -show drun
$browser = thorium-browser
$editor = nvim

#################
### AUTOSTART ###
#################

# Autostart necessary processes (like notifications daemons, status bars, etc.)
# Or execute your favorite apps at launch like this:

# exec-once = $terminal
exec-once = nm-applet &
exec-once = udiskie -a -n -t &
exec-once = swww-daemon &
exec-once = swaync &
exec-once = $HOME/scripts/waybar.sh  
exec-once = $HOME/scripts/randomwall.sh &
exec-once = $HOME/scripts/portal.sh &
exec-once = xhost +SI:localuser:root
#exec-once = blueman-applet &
exec-once = /usr/lib/polkit-kde-authentication-agent-1
# Clipboard Manager
exec-once = wl-paste --type text --watch cliphist store #Stores only text data
exec-once = wl-paste --type image --watch cliphist store #Stores only image data
exec-once = paru -Qe > ~/installed.txt &&
exec-once = hypridle
#############################
### ENVIRONMENT VARIABLES ###
#############################

# See https://wiki.hyprland.org/Configuring/Environment-variables/

env = XCURSOR_SIZE,24
env = HYPRCURSOR_SIZE,24


#####################
### LOOK AND FEEL ###
#####################

# Refer to https://wiki.hyprland.org/Configuring/Variables/

# https://wiki.hyprland.org/Configuring/Variables/#general
general { 
    gaps_in = 5
    gaps_out =2
    col.inactive_border = $colorborder
    col.active_border = $coloractiveborder
    border_size =2
    # https://wiki.hyprland.org/Configuring/Variables/#variable-types for info about colors
    # Set to true enable resizing windows by clicking and dragging on borders and gaps
    resize_on_border = true    # Please see https://wiki.hyprland.org/Configuring/Tearing/ before you turn this on
    allow_tearing = false
    layout = dwindle
}

# https://wiki.hyprland.org/Configuring/Variables/#decoration
decoration {
  col.shadow = $colorshadow
  col.shadow_inactive = $colorshadowinactive
    rounding = 15
    # Change transparency of focused and unfocused windows
    active_opacity = 1.0
    inactive_opacity =0.9
    drop_shadow = true
    shadow_range = 30
    shadow_render_power = 4
   blur {
        enabled = true
        xray = false
        size = 10
        noise =0.1 
        contrast = 2
        popups = false
        special = false
        passes = 4
        new_optimizations = true
    }
}

# https://wiki.hyprland.org/Configuring/Variables/#animations
animations {
    enabled = yes
  #bezier = overshot, 0.48,0.31,0.66,1.42)
  #bezier = quart, 0.25, 1, 0.5, 1
    bezier = myBezier, 0.05, 0.9, 0.1, 1.05

    animation = windows, 1, 7, myBezier
    animation = windowsOut, 1, 7, default, popin 80%
    animation = border, 1, 10, default
    animation = borderangle, 1, 8, default
    animation = fade, 1, 7, default
    animation = workspaces, 1, 6, default
}
# See https://wiki.hyprland.org/Configuring/Dwindle-Layout/ for more
dwindle {
    pseudotile = true # Master switch for pseudotiling. Enabling is bound to mainMod + P in the keybinds section below
    preserve_split = true # You probably want this
}


# https://wiki.hyprland.org/Configuring/Variables/#misc
misc { 
    force_default_wallpaper = 1 # Set to 0 or 1 to disable the anime mascot wallpapers
    disable_hyprland_logo = true # If true disables the random hyprland logo / anime girl background. :(
}


#############
### INPUT ###
#############

# https://wiki.hyprland.org/Configuring/Variables/#input
input {
    kb_layout = br, us
    kb_options=grp:win_space_toggle
    kb_variant =
    kb_model =
    kb_rules =
    scroll_factor = 0.1 
    follow_mouse = 1

    sensitivity = 0 # -1.0 - 1.0, 0 means no modification.

    touchpad {
        natural_scroll = false
    }
}

# https://wiki.hyprland.org/Configuring/Variables/#gestures
gestures {
    workspace_swipe = true
}

# Example per-device config
# See https://wiki.hyprland.org/Configuring/Keywords/#per-device-input-configs for more
device {
    name = epic-mouse-v1
    sensitivity = -0.5
}


####################
### KEYBINDINGSS ###
####################

# See https://wiki.hyprland.org/Configuring/Keywords/
$mainMod = SUPER # Sets "Windows" key as main modifier
$mainModShift = SUPER+SHIFT # Sets "Windows" key + Shift as main modifier
$mainModCtrl = SUPER+CONTROL # Sets "Windows" key + Ctrl as main modifie
$mainModAlt = SUPER+ALT # Sets "Windows" key + Alt as main modifier

# Example binds, see https://wiki.hyprland.org/Configuring/Binds/ for more
bind = $mainModShift, R, exec, hyprctl reload
bind = $mainMod, E, exec, $terminal
bind = $mainMod, Q, killactive,
bind = $mainModshift, Q, exec, hyprctl activewindow | awk '/pid:/ {print $2}' | xargs kill -9
bind = $mainMod, delete, exec,~/scripts/powermenu.sh
bind = $mainModshift, delete,  exit
bind = mainModAlt, delete,exec, killall -q -9 Hyprland
bind = $mainMod, D, exec, $fileManager
bind = $mainModAlt, D, exec, dolphin 
bind = $mainModshift, D, exec, $terminal yazi 
bind = $mainMod, V, togglefloating,
bind = $mainMod, R, exec, $menu
bind = $mainModCtrl, R, exec, $HOME/scripts/rofi-desktop/scripts/rofi-desktop.sh
bind = $mainMod, P, pseudo, # dwindle
bind = $mainMod, -, togglesplit, # dwindle
bind = $mainMod, W, exec,  $browser
bind = $mainModShift, W, exec, $browser --incognito weboasis.app
bind = $mainMod, B, exec, pkill -SIGUSR1 '^waybar$'
bind = $mainModshift, B, exec,sh  ~/scripts/waybar.sh
bind = $mainMod, F, fullscreen,
bind = $mainMod, C, pin,
bind = $mainMod, X,exec,sh ~/scripts/randomwall.sh
#Clipboard manager and prints
bind = $mainModShift, V, exec, rofi -modi clipboard:~/scripts/cliphist-rofi-img.sh -show clipboard -show-icons

bind = $mainModShift, Print, exec, grim -g "$(slurp)" $HOME/Pictures/Screenshots/$(date +'%s_grim.png') && LATEST=$HOME/Pictures/Screenshots/$(ls -t $HOME/Pictures/Screenshots/ | head -n1) && wl-copy < "$LATEST" && notify-send -i "$LATEST" "Screenshot Saved" "Screenshot saved and copied to clipboard."
bind = $mainMod, Print, exec, grim $HOME/Pictures/Screenshots/$(date +'%s_grim.png') && LATEST=$HOME/Pictures/Screenshots/$(ls -t $HOME/Pictures/Screenshots/ | head -n1) && wl-copy < "$LATEST" && notify-send -i "$LATEST" "Screenshot Saved" "Screenshot saved and copied to clipboard."
bind = $mainModCtrl, Print, exec, grim -g "$(slurp)" $HOME/Pictures/Screenshots/$(date +'%s_grim.png') && LATEST=$HOME/Pictures/Screenshots/$(ls -t $HOME/Pictures/Screenshots/ | head -n1) && TEXT=$(tesseract -l "por+eng" "$LATEST" stdout) && echo "$TEXT" | wl-copy && notify-send -i "$LATEST" "Copied" "$TEXT"
##bind = $mainModCtrl, Print, exec , grim -g "$(slurp -o)" $HOME/Pictures/Screenshots/$(date +'%s_grim.png')
# Move focus with mainMod + arrow keys
bind = $mainMod, h, movefocus, l
bind = $mainMod, l, movefocus, r
bind = $mainMod, k, movefocus, u
bind = $mainMod, j, movefocus, d

# Move focused window around the current workspace
bind = $mainModShift+Ctrl, h, movewindow, l
bind = $mainModShift+Ctrl, l, movewindow, r
bind = $mainModShift+Ctrl,k , movewindow, u
bind = $mainModShift+Ctrl,j, movewindow, d

# Switch workspaces with mainMod + [0-9]
bind = $mainMod, 1, workspace, 1
bind = $mainMod, 2, workspace, 2
bind = $mainMod, 3, workspace, 3
bind = $mainMod, 4, workspace, 4
bind = $mainMod, 5, workspace, 5
bind = $mainMod, 6, workspace, 6
bind = $mainMod, 7, workspace, 7
bind = $mainMod, 8, workspace, 8
bind = $mainMod, 9, workspace, 9
bind = $mainMod, 0, workspace, 10
bind=$mainMod,TAB,workspace,previous
bind = $mainModAlt, L, workspace, +1
bind = $mainModAlt, H, workspace, -1 



# Move active window to a workspace with mainMod + SHIFT + [0-9]
bind = $mainModShift, 1, movetoworkspace, 1
bind = $mainModShift, 2, movetoworkspace, 2
bind = $mainModShift, 3, movetoworkspace, 3
bind = $mainModShift, 4, movetoworkspace, 4
bind = $mainModShift, 5, movetoworkspace, 5
bind = $mainModShift, 6, movetoworkspace, 6
bind = $mainModShift, 7, movetoworkspace, 7
bind = $mainModShift, 8, movetoworkspace, 8
bind = $mainModShift, 9, movetoworkspace, 9
bind = $mainModShift, 0, movetoworkspace, 10
# move focus between Monitors
bind = $mainModCtrl, K, focusmonitor, +1
bind = $mainModCtrl, J, focusmonitor, -1

# Example special workspace (scratchpad) bind = $mainMod, S, togglespecialworkspace, magic
bind = $mainModShift, S, movetoworkspace, special:magic
bind = $mainMod, S, togglespecialworkspace,magic
# Scroll through existing workspaces with mainMod + scroll
bind = $mainMod, mouse_down, workspace, e+1
bind = $mainMod, mouse_up, workspace, e-1
# Move/resize windows with mainMod + LMB/RMB and dragging
bindm = $mainMod, mouse:272, movewindow
bindm = $mainMod, mouse:273, resizewindow
binde = $mainModShift, l, resizeactive, 30 0
binde = $mainModShift, h, resizeactive, -30 0
binde = $mainModShift, k, resizeactive, 0 -30
binde = $mainModShift, j, resizeactive, 0 30
# Scroll through existing workspaces
bind = $mainMod, mouse_down, workspace, e+1
bind = $mainMod, mouse_up, workspace, e-1
# Move/Resize focused window
bindm = $mainMod, mouse:272, movewindow
bindm = $mainMod, mouse:273, resizewindow
bindm = $mainMod, M, movewindow
bindm = $mainMod, N, resizewindow
# Toggle focused window split
bind = $mainMod, T, togglesplit
# Move focused window to a workspace silently
bind = $mainModAlt, 1, movetoworkspacesilent, 1
bind = $mainModAlt, 2, movetoworkspacesilent, 2
bind = $mainModAlt, 3, movetoworkspacesilent, 3
bind = $mainModAlt, 4, movetoworkspacesilent, 4
bind = $mainModAlt, 5, movetoworkspacesilent, 5
bind = $mainModAlt, 6, movetoworkspacesilent, 6
bind = $mainModAlt, 7, movetoworkspacesilent, 7
bind = $mainModAlt, 8, movetoworkspacesilent, 8
bind = $mainModAlt, 9, movetoworkspacesilent, 9
bind = $mainModAlt, 0, movetoworkspacesilent, 10
#notebook fn keys
bind = ,XF86AudioLowerVolume, exec, pactl set-sink-volume @DEFAULT_SINK@ -5%
bind = ,XF86AudioRaiseVolume, exec, pactl set-sink-volume @DEFAULT_SINK@ +5%
bind = $mainMod, F1 , exec, pactl set-source-mute 0 toggle
bind = ,XF86AudioMute,exec, pactl set-sink-mute @DEFAULT_SINK@ toggle
bind = ,XF86MonBrightnessUp, exec, brightnessctl set 5%+
bind = ,XF86MonBrightnessDown, exec, brightnessctl set 5%-
bind = ,XF86AudioPlay, exec, playerctl play-pause
bind = ,XF86AudioNext, exec, playerctl next
bind = ,XF86AudioPrev, exec, playerctl previous
bind = ,XF86AudioStop, exec, playerctl stop
bind = $mainMod, F2, exec, playerctl play-pause
#pamac
bind = $mainMod, Y, exec,pamac-manager
#debug
bind = $mainMod, F5, exec, $terminal wev
bind = $mainModCtrl, D, exec, wlrlui
#overview and app drawer
#bind = $mainModshift, Space, overview:toggle
bind = $mainModAlt, Space, exec, nwg-drawer
#disable animations
bind=$mainMod,G,exec,~/scripts/gamemode.sh
bind = $mainMod,F10,pass,^(com\.obsproject\.Studio)$
#editor
bind = $mainModshift,E , exec, $terminal $editor
#emoji picker
bind = $mainModshift, M, exec, rofi -modi emoji -show emoji

#############################
### WINDOWS AND WORKSPACES ###
##############################

# See https://wiki.hyprland.org/Configuring/Window-Rules/ for more
# See https://wiki.hyprland.org/Configuring/Workspace-Rules/ for workspace rules

# Example windowrule v1
# windowrule = float, ^(kitty)$
# Example windowrule v2
# windowrulev2 = float,class:^(kitty)$,title:^(kitty)$
windowrulev2 = suppressevent maximize, class:.* # You'll probably like this.




#settag
windowrule = float, ^(.*)(V4L2)(.*)$
windowrule = pin,title:^(.*)(V4L2)(.*)$
windowrule = tag +nopretty, ^(.*)(Show Me The Key)(.*)$
windowrule = pin, ^(.*)(Show Me The Key)(.*)$
windowrule = float, ^(.*)(Show Me The Key)(.*)$
windowrule = tag +nopretty, ^(.*)(blobdrop)(.*)$
windowrule = float, ^(blobdrop)$
windowrule = pin, ^(blobdrop)$
windowrule = move 5%- , ^(blobdrop)$
windowrule = move 5%-,title:^(.*)(V4L2)(.*)$
windowrule = tag +nopretty, ^(.*)(Gromit-mpx)(.*)$
windowrule = tag +nopretty, ^(.*)(Qalc)(.*)$
windowrule = float, ^(.*)(Qalculate!)(.*)$


####tag
windowrulev2 = opacity 1 override 1 override 1 override,tag:nopretty
windowrulev2 = noblur,tag:nopretty

workspace = special:gpt, gapsin:0, gapsout:0, on-created-empty: $browser --app=https://www.chatgpt.com/
workspace = special:gemini, gapsin:0, gapsout:0, on-created-empty: $browser --app=https://gemini.google.com/app
workspace = special:whatsapp, gapsin:0, gapsout:0, on-created-empty: $browser --app=https://web.whatsapp.com/
workspace = special:nvim, gapsin:0, gapsout:0, on-created-empty: $terminal nvim '+set bt=nofile' FILE 

bind = $mainMod, A, togglespecialworkspace, gpt
bind = $mainModshift, A, togglespecialworkspace, gemini
bind = $mainMod, z, togglespecialworkspace, whatsapp
bind = $mainModshift, N, togglespecialworkspace, nvim

#Picture-in-Picture special treatment
windowrulev2 = float,title:(Picture-in-Picture)
windowrulev2 = size 585 330,title:(Picture-in-Picture),
windowrulev2 = move 100%-816 50,title:(Picture-in-Picture),
windowrulev2 = pin,title:(Picture-in-Picture),
windowrulev2 = nodim,title:(Picture-in-Picture)
windowrulev2 = opacity 1 0.8,title:(Picture-in-Picture),
windowrulev2 = noinitialfocus,title:(Picture-in-Picture),
windowrulev2 = opacity 1,title:(Picture-in-Picture),floating:0

#layerrules
#layerrule = blur, waybar
layerrule = unset, rofi
layerrule = blur, rofi
layerrule = ignorezero, rofi
layerrule = blur, swaync-control-center
layerrule = blur, swaync-notification-window
layerrule = ignorezero, swaync-control-center
layerrule = ignorezero, swaync-notification-window
layerrule = ignorealpha 0.1, swaync-control-center
layerrule = ignorealpha 0.1, swaync-notification-window
##gromit related stuff

workspace = special:gromit, gapsin:0, gapsout:0, on-created-empty: gromit-mpx -a

windowrule = noblur, ^(Gromit-mpx)$

windowrule = opacity 1 override, 1 override, ^(Gromit-mpx)$

windowrule = noshadow, ^(Gromit-mpx)$

bind = $mainModAlt, G, togglespecialworkspace, gromit

bind = , F8, exec, gromit-mpx --undo

bind = SHIFT , F8, exec, gromit-mpx --redo
##plugins


